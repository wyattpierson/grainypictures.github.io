use strict;

$private{ 'force_user_limits' } = sub {
	my ($b_verbose, $b_status, %user) = @_;
	my $err = '';
	Err: {

		# consult the help docs for the specification of this sub (to-be-written as of 2002-07-12)

		last Err;
		}
	return ($err, %user);
	};

1;
